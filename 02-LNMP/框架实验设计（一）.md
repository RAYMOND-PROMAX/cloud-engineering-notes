# 实验环境
## 所需虚拟机
| 主机名 | IP地址 | 主要角色 | 软件 | 作用 |
| --- | --- | --- | --- | --- |
| **LB01** | `10.0.0.6`<br/>172.16.1.6<br/> | 负载均衡 | Nginx + check模块 | 接收用户请求、负载均衡、健康检查 |
| **WEB01** | `10.0.0.7`<br/>172.16.1.7 | Web服务器 | Nginx + PHP-FPM + WordPress + phpMyAdmin | 提供Web业务 |
| **WEB02** | `10.0.0.8`<br/>172.16.1.8 | Web服务器 | Nginx + PHP-FPM + WordPress + phpMyAdmin | 提供Web业务 |
| **NFS** | `10.0.0.31`<br/>172.16.1.31 | 共享存储 | NFS | 为WEB01/WEB02提供共享文件 |
| **DB01** | `10.0.0.51`<br/>172.16.1.51 | 数据/会话服务器 | MariaDB + Redis | MariaDB保存业务数据，Redis保存PHP Session |




| 层 | 主机 | 业务网 | 内网/后端网 | 主要服务 |
| --- | --- | --- | --- | --- |
| **WAF** | **201** | **10.0.0.201** | **172.16.1.201** | WAF + 蜜罐 |
| **LB** | **LB01** | **10.0.0.6** | **172.16.1.6** | Nginx / LB |
| **LB** | **LB02** | **10.0.0.5** | **172.16.1.5** | Nginx / LB |
| **LB VIP** | **VIP** | **10.0.0.3** | — | LB 高可用入口 |
| **WEB** | **WEB01** | **10.0.0.7** | **172.16.1.7** | Nginx + PHP-FPM + WordPress |
| **WEB** | **WEB02** | **10.0.0.8** | **172.16.1.8** | Nginx + PHP-FPM + WordPress |
| **DB** | **DB01** | **10.0.0.51** | **172.16.1.51** | MariaDB + Redis |
| **存储** | **NFS** | **10.0.0.31** | **172.16.1.31** | NFS |
| **备份** | **Backup** | **10.0.0.41** | **172.16.1.41** | Rsync |


## 网络环境准备
```plain
hostnamectl set-hostname 主机名

vim /etc/sysconfig/network-scripts/ifcfg-ens33
vim /etc/sysconfig/network-scripts/ifcfg-ens36


ifdown ens36&&ifup ens36
ifdown ens33&&ifup ens33
```



# 设计思路
```plain
LB01
  │
  ├── WEB01
  └── WEB02
       │
       ├── NFS
       └── DB01
            ├── MariaDB
            └── Redis
                         
                         用户
                          │
                          │ HTTP
                          ▼
                    ┌───────────┐
                    │   LB01    │
                    │ Nginx+LB  │
                    │  + check  │
                    └─────┬─────┘
                          │
                   负载均衡分发
                    ┌─────┴─────┐
                    ▼           ▼
                  WEB01       WEB02
                    │           │
                    └─────┬─────┘
                          │
                    Nginx + PHP-FPM
                          │
                          ▼
                     WordPress
                          │
             ┌────────────┼────────────┐
             │            │            │
             ▼            ▼            ▼
          MariaDB       Redis         NFS
             │            │            │
             │            │            │
             ▼            ▼            ▼
          业务数据      Session       上传文件
             │            │            │
             └────────────┴────────────┘
                          │
                          ▼
                       DB01/NFS

     
```



# 配置环境
## **NFS**
### 安装服务
```plain
[root@nfs ~]# yum -y install nfs-utils
```

### 配置匿名用户以及共享目录
```plain
#配置文件/etc/exports

[root@nfs ~]# cat /etc/exports
/data 172.16.1.0/24(rw,sync,all_squash)

#all_squash 将所有远程用户身份压缩成匿名用户

#根据配置文件创建必要数据信息
[root@nfs ~]# mkdir /data
```

### 启动服务验证
```plain
[root@nfs ~]# systemctl start nfs
[root@nfs ~]# systemctl enable nfs


```

### 检查
#### 服务端检查
```plain
#etab文件中如果有内容说明配置文件是正确的，如果是空的说明exports配置文件配置错误
[root@nfs ~]# cat /var/lib/nfs/etab 
/data	172.16.1.0/24(rw,sync,wdelay,hide,nocrossmnt,secure,root_squash,all_squash,no_subtree_check,secure_locks,acl,no_pnfs,anonuid=65534,anongid=65534,sec=sys,rw,secure,root_squash,all_squash)

```

#### 客户端查看服务端共享的目录
```plain
#注意showmount命令如果没有 需要安装nfs-utils ,客户端不需要启动nfs服务
[root@web01 ~]# showmount -e 172.16.1.31
Export list for 172.16.1.31:
/data 172.16.1.0/24

```



## LB01
### 安装nginx以及编译check
略

### 修改配置文件
/etc/nginx/conf.d/wp.conf 

```plain
[root@lb01 conf.d]# pwd
/etc/nginx/conf.d
[root@lb01 conf.d]# cat wp.conf 
upstream wp {
    server 172.16.1.7:80 max_fails=2 fail_timeout=10s;
    server 172.16.1.8:80 max_fails=2 fail_timeout=10s;
    check interval=3000 rise=2 fall=3 timeout=1000 type=tcp;
    #interval  检测间隔时间，单位为毫秒
    #rise      表示请求2次正常，标记此后端的状态为up
    #fall      表示请求3次失败，标记此后端的状态为down
    #type      类型为tcp
    #timeout   超时时间，单位为毫秒
}
server {
	listen 80;
	server_name www.wp.com;

	location / {
	proxy_pass http://wp;
	include proxy;
	proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
	}
    location /check {
        check_status;
    }

}

```



## DB01
### 安装数据库
```plain
yum -y install mariadb-server

[root@web01 ~]# systemctl start mariadb
[root@web01 ~]# systemctl enable mariadb

修改密码[root@web01 ~]# mysqladmin password 'lzy123.com'

[root@web01 ~]# mysql -uroot -p'lzy123.com'

```

#### 创建用户
```plain
[root@db01 ~]# mysql -urlzy -plzy123.com
Welcome to the MariaDB monitor.  Commands end with ; or \g.
Your MariaDB connection id is 12
Server version: 10.3.39-MariaDB MariaDB Server

Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> grant all on  *.* to lzy@'%' identified by 'lzy123.com';
Query OK, 0 rows affected (0.000 sec)

MariaDB [(none)]> 
```

#### 远程连接测试
```plain
#创建完成后测试web01通过lzy连接db01
[root@web01 ~]# mysql -h172.16.1.51 -ulzy -plzy123.com
Welcome to the MariaDB monitor.  Commands end with ; or \g.
Your MariaDB connection id is 13
Server version: 10.3.39-MariaDB MariaDB Server

Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> show databases;
```



### 安装redis
```plain
#部署redis
#安装
[root@db01 ~]# yum -y install redis
```

#### 修改配置文件
```plain
[root@db01 ~]# egrep '123456|172.16' /etc/redis.conf
bind 127.0.0.1 172.16.1.51
requirepass 123456
```

#### 启动测试
```plain
#启动redis
[root@db01 ~]# systemctl start redis
[root@db01 ~]# systemctl enable redis

[root@db01 ~]# netstat -tnulp
Active Internet connections (only servers)
Proto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program name    
tcp        0      0 172.16.1.51:6379        0.0.0.0:*               LISTEN      7597/redis-server 1 
tcp        0      0 127.0.0.1:6379          0.0.0.0:*               LISTEN      7597/redis-server 1


登录测试
[root@db01 ~]# redis-cli -h 127.0.0.1 -a '123456'
127.0.0.1:6379> keys *
```

## web01&&web02
### php服务配置
#### **安装php服务**
```plain
yum -y install php php-bcmath php-cli php-common php-devel php-embedded php-fpm php-gd php-intl php-mbstring php-mysqlnd php-opcache php-pdo   php-process php-xml php-json
```

#### **修改监听方式**
```plain
[root@web01 ~]# grep 9000 /etc/php-fpm.d/www.conf 
listen = 127.0.0.1:9000
```

#### **启动与开机自启动**
```plain
[root@web01 ~]# systemctl start php-fpm
[root@web01 ~]# systemctl enable php-fpm
```

#### 编译安装redis组件
```plain
#WEB01客户端需要安装redis模块
第一步: 下载redis源码包
[root@web01:~]# wget https://pecl.php.net/get/redis-5.3.7.tgz
第二步： 解压代码
[root@web01:~]# tar -zxvf redis-5.3.7.tgz
第三步： 配置
[root@web01:~]#cd redis-5.3.7/
[root@web01 redis-5.3.7]# phpize
[root@web01:redis-5.3.7]#./configure
第四步： 编译安装
[root@web01:redis-5.3.7]# make && make install
第五步： 开启redis插件功能，配置文件增加以下一行内
容
[root@web01:~]#grep redis.so /etc/php.ini  -n
1357:extension=redis.so

[root@web01 ~]# php-fpm -t
[15-Aug-2025 16:16:27] NOTICE:
configuration file /etc/php-fpm.conf test
is successful


最后重启php生效
[root@web01 redis-5.3.7]# systemctl restart php-fpm
```

### 部署Mariadb
#### 安装启动mariadb服务
```plain
[root@web01 ~]# yum -y install mariadb-server

[root@web01 ~]# systemctl start mariadb
[root@web01 ~]# systemctl enable mariadb
```



#### 修改登录密码
```plain
[root@web01 ~]# mysqladmin password 'lzy123.com'
```



#### 登录测试
```plain
[root@web01 ~]# mysql -uroot -p'lzy123.com'

MariaDB [(none)]> show databases;
+--------------------+
| Database           |
+--------------------+
| information_schema |
| mysql              |
| performance_schema |
+--------------------+
3 rows in set (0.001 sec)

MariaDB [(none)]> quit
```

#### 创建数据库
```plain
create database wp;
```



### 部署代码
#### nginx配置文件
```plain
[root@web01 conf.d]# cat zh.conf
server {
        listen 80;
        server_name www.zh.com;

        location / {
         root /code/zh;
         index index.php index.html;
        }

        location ~ \.php$ {
         root /code/zh;
         fastcgi_pass 127.0.0.1:9000;
         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
	 include fastcgi_params;
        }
}
```

#### 源码
```plain
拉去
wget https://cn.wordpress.org/wordpress-6.9.7-zh_CN.tar.gz

解压
tar xf wordpress-6.9.7-zh_CN.tar.gz 

修改wordpress属主属主为apache
[root@web01 code]# chown -R apache.apache wordpress

```



### 拆分存储（远程存储）
#### 动态数据
##### 导入的数据
```plain
[root@web01 ~]# mysqldump -uroot -plzy123.com -A > all.sql
[root@web01 ~]# scp all.sql 10.0.0.51:~

[root@db01 ~]# mysql -uroot -plzy123.com < all.sql
[root@db01 ~]# systemctl restart mariadb


```

##### 修改指向文件
```plain
[root@web01 ~]# cd /code/wordpress/
[root@web01 wordpress]# vim wp-config.php
...
define( 'DB_NAME', 'wp' );

/** Database username */
define( 'DB_USER', 'lzy' );			# 修改为远程连接用户
  
/** Database password */
define( 'DB_PASSWORD', 'lzy123.com' );

/** Database hostname */
define( 'DB_HOST', '172.16.1.51' );	# 修改为新的数据库的服务器地址
....


```



#### 静态数据
##### 直接挂载
```plain
mount -t nfs 172.16.1.31:/data /code/wordpress/wp-content/uploads/
```

##### 永久挂载
```plain
[root@web01 mnt]# tail -1 /etc/fstab
172.16.1.31:/data       /mnt                    nfs     defaults 0 0
```



#### cookie文件
修改配置文件指向redis

```plain
#WEB01配置将会话指向到redis 修改php.ini文件
[root@web01 ~]# sed -n '1222p;1255p' /etc/php.ini
session.save_handler = redis
session.save_path = "tcp://172.16.1.51:6379?auth=123456"
```

```plain
#将后4行注释
[root@web01 ~]# tail -5 /etc/php-fpm.d/www.conf
; at http://php.net/session.save-path
;php_value[session.save_handler] = files
;php_value[session.save_path]    = /var/lib/php/session
;php_value[soap.wsdl_cache_dir]  = /var/lib/php/wsdlcache
;php_value[opcache.file_cache]  = /var/lib/php/opcache
```















